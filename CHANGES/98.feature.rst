Deferred construction of the owned ``AsyncZeroconf`` instance until the first
``.local`` lookup, so resolvers that only handle unicast names no longer open
mDNS multicast sockets -- by :user:`aiolibsbot`.
